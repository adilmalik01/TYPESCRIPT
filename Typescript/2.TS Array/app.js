console.log("intialize reposatiry in github");
console.log('Hello adil');
// Numbers array 
//let numArr: number[] = [1, 2, 3, 4, 5] // this array is only contain numbers array
//numArr.push(2) // this is add one value in end of array
//numArr.unshift(0) // this is add one value in start of array
//numArr.pop(1) // this is remove one value in last of array
// numArr.shift(0,1)
// numArr.splice(2,2) // this is remove item center in array with index number
// let newNum:number[] = numArr.slice(0,1) // this is copy any item in array 0 > 1
// console.log(newNum);
// console.log(numArr);
// let strArray: string[] = ["Adil", "abdullah", "Asif"]
var palindRome = "Madam";
palindRome = palindRome.toLocaleLowerCase(); // this is convert all string in lowerCase
var palindRomeConvert = palindRome.split(""); // this is convert the string to array
palindRomeConvert.reverse(); // this funtion reverse array
var finalPalinRome = palindRomeConvert.join(""); // and this is covert array to string
console.log(finalPalinRome);
